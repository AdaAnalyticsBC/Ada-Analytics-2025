[build]
builder = "nixpacks"

[deploy]
startCommand = "yarn run build && yarn run preview --host 0.0.0.0 --port $PORT"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10

[deploy.environment]
NODE_ENV = "production" 